Move everything in the top level `provider` repo except 'packages/' to this folder.

This will create the package structure:

provider/
  packages/
    provider/
       ... (everything else)
    provider_devtools_extension/
    